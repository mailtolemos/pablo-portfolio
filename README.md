# Pablo

Private wealth intelligence across crypto, equities, DeFi and macro markets.

## Local preview

Open `outputs/pablo.html` in a browser.

## Deployment

The project is configured as a static Vercel deployment. The root route serves
the Pablo dashboard and rewrites its image assets from `outputs/assets`.

